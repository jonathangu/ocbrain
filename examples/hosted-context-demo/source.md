# OCBrain public installation facts

OCBrain requires Python 3.11 or newer and a Git source installation.

One compatible stdio MCP client is enough. OpenClaw is optional.

The source launcher targets macOS and Linux. Native Windows is unsupported;
WSL has not passed release acceptance.

The local SQLite database is plaintext and should remain owner-only.
